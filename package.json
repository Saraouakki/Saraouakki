{
  "name": "ecc",
  "version": "0.1.0",
  "description": "ECC — Everything Claude Code: an agent harness performance system (memory, skills, agents, continuous learning, security scanning).",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ecc": "./bin/ecc.js"
  },
  "files": [
    "bin",
    "dist",
    "skills",
    "agents"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "ecc": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
